server:
  port: 8081

logging:
  level:
    root: DEBUG

spring:
  output:
    ansi:
      enabled: always
  data:
    mongodb:
      host: localhost
      port: 27017
      database: mangodb
  reactive:
    mongodb:
      repositories:
        enabled: true
